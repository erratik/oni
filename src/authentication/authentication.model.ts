export interface UbiServiceS2SSessionData {
  applicationId: string;
  clientIp: string;
  environment: string;
  expiration: string;
  serverTime: string;
  sessionId: string;
  sessionKey: string;
  spaceId: string;
  ticket: string;
}
