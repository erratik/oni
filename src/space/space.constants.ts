export enum QueryRequestSources {
  Instagram = 'instagram',
}

export enum DataMethod {
  instagram = 'get',
  spotify = 'post',
  googleapi = 'post',
  spotify_default = 'get',
  googleapi_location = 'get',
}
